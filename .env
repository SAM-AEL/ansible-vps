# .env - configuration for ansible-vps
# Edit values below to customize the playbook behavior.
# Do NOT store secrets in this file for production; use Ansible Vault instead.

# Primary admin user to add to docker group and to run services (default: sam)
VPS_ADMIN_USER=sam

# Primary domain name for nginx / certbot (leave empty if not using)
DOMAIN=

# Email used for Let's Encrypt certificate registration
EMAIL=admin@example.com

# Webhook listener settings
WEBHOOK_PORT=9000
WEBHOOK_USER=sam

# Ports to allow through UFW, comma separated
UFW_ALLOW_PORTS=22,80,443

# Docker packages (used as reference)
DOCKER_PACKAGES="docker-ce docker-ce-cli containerd.io docker-compose-plugin"

# Certbot DNS plugin toggle and provider credentials placeholder
CERTBOT_DNS_CLOUDFLARE=false
CLOUDFLARE_API_TOKEN=

# Playbook file to run (relative to repo root)
ANSIBLE_PLAYBOOK=playbook.yml

# Notes:
# - Change values, then run the helper script `run-ansible.sh` or source this file before running Ansible.
# - For secrets (API tokens, private keys) use Ansible Vault or an external secret manager.
